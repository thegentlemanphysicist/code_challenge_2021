name: Manifest Generation

on:
  release:
    types: [published]
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run one line
      run:  echo Hello, world!
