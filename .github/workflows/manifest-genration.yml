name: Manifest Generation

on:
  release:
    types: [published]
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Write File
      uses: DamianReeves/write-file-action@v1.0
      with:
       path: ${{ env.home}}/manifest.md
       contents: |
         echo "Hello World!"
       write-mode: append

